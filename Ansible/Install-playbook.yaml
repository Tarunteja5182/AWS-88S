- name: Installation
  hosts: all
  become: true
  tasks:
   - name: Install Nginx
     ansible.builtin.package:
      name : Nginx
      state: present
   - name: Enable Nginx
     ansible.builtin.service:
      name: Nginx
      state: started
      enabled: yes

     