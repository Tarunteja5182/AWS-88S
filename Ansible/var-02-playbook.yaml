- name : Vairable Presidence
  hosts: local
  connection: local
  vars:
   txt: "This is a play level variable"
  # vars_files:
  #  - vars-play
  vars_prompt:
   - prompt : "please enter txt"
     private : false
  tasks:
   - name: Proof of variable Presidence
    #  vars:
    #   txt: "This is a task level variable"
     ansible.builtin.debug:
      msg: "{{ txt }}"
   
